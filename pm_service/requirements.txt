# ==================== Flask 核心框架 ====================
Flask==3.0.3
Werkzeug==3.0.4
Jinja2==3.1.6
click==8.1.7
itsdangerous==2.2.0
blinker==1.8.2

# ==================== Flask 扩展 ====================
Flask-Cors==5.0.0
flask-jwt-extended==4.5.3
flask-marshmallow==1.2.1
Flask-Migrate==4.0.7
flask-smorest==0.44.0
Flask-SQLAlchemy==3.1.1
Flask-Limiter>=2.0.0

# ==================== 数据库相关 ====================
SQLAlchemy==2.0.34
alembic==1.13.2
PyMySQL==1.1.1
greenlet==3.1.0
pymongo>=4.6.0              # MongoDB 驱动
influxdb-client>=1.36.0     # InfluxDB 时序数据库客户端
elasticsearch>=8.0.0        # Elasticsearch 搜索引擎客户端

# ==================== 缓存相关 ====================
redis==5.0.8
async-timeout==4.0.3

# ==================== 任务队列 ====================
celery>=5.3.0              # 分布式任务队列
kombu>=5.3.0               # Celery 消息传递库
vine>=5.0.0                # Celery 依赖
flower>=2.0.0              # Celery 监控工具（可选）

# ==================== 消息队列 ====================
pika>=1.3.0                # RabbitMQ 客户端

# ==================== 文件存储相关 ====================
boto3>=1.34.0              # AWS S3 客户端
botocore>=1.34.0           # AWS 核心库
minio>=7.1.0               # MinIO 客户端

# ==================== 网络和文件传输 ====================
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.3.2
idna==3.9

# ==================== 序列化和验证 ====================
marshmallow==3.22.0
apispec==6.6.1
webargs==8.6.0
PyYAML>=6.0

# ==================== 认证和加密 ====================
PyJWT==2.10.0
cryptography>=41.0.0       # Fernet 对称加密

# ==================== 模板和配置 ====================
Mako==1.3.5
MarkupSafe==2.1.5
packaging==24.1
python-dotenv>=1.0.0       # .env 文件加载

# ==================== 日志和监控 ====================
structlog>=23.1.0
concurrent-log-handler>=0.9.20

# ==================== 类型提示 ====================
typing_extensions==4.12.2
pydantic>=2.0.0

# ==================== Web 服务器 ====================
waitress==3.0.0

# ==================== 其他工具 ====================
colorama==0.4.6

# ==================== 系统内置库（无需安装） ====================
# ftplib              # Python 标准库
# zipfile             # Python 标准库
# configparser        # Python 标准库
# pathlib             # Python 标准库
# functools           # Python 标准库
# datetime            # Python 标准库
# json                # Python 标准库
# os                  # Python 标准库
# re                  # Python 标准库
# io                  # Python 标准库
# traceback           # Python 标准库
# inspect             # Python 标准库
# importlib           # Python 标准库

# ==================== 开发环境依赖 ====================
# pytest              # 测试框架（可选）
# coverage            # 代码覆盖率（可选）
# black               # 代码格式化（可选）
# flake8              # 代码检查（可选）
# mypy                # 类型检查（可选）

# ==================== 生产环境依赖 ====================
# gunicorn            # WSGI 服务器（生产环境推荐）
# gevent              # 异步处理（可选）
# supervisor          # 进程管理（可选）

# ==================== 数据库驱动（按需选择） ====================
# cx_Oracle           # Oracle 数据库驱动（如需 Oracle 支持）
# psycopg2-binary     # PostgreSQL 数据库驱动（如需 PostgreSQL 支持）
# pymssql             # SQL Server 数据库驱动（如需 SQL Server 支持）